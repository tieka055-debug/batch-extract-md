# Provenance

This project is independently implemented. Its browser-rendered export workflow was informed by the public behaviour demonstrated by `yiyunwei10-oss/extract-getnote-spa-version` (MIT licensed); no source files are copied. `Likely7/GetbijiEx` was reviewed for product-level ideas only because the repository currently does not include a licence granting reuse.
