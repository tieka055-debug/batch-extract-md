Metadata-Version: 2.4
Name: batch-extract-md
Version: 0.1.0
Summary: 批量提取文案md：将 Get笔记知识库内容导出为 Markdown
Requires-Python: >=3.11
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: playwright>=1.40
Dynamic: license-file

# 批量提取文案md

> **批量提取文案md** 是一个本地运行的 Get笔记（biji.com）知识库 Markdown 导出工具。
>
> 它通过独立的浏览器完成登录和页面操作，把你已经可以正常查看的知识库文章批量整理成可搜索、可备份、可继续编辑的 `.md` 文件。

[![CI](https://github.com/tieka055-debug/batch-extract-md/actions/workflows/ci.yml/badge.svg)](https://github.com/tieka055-debug/batch-extract-md/actions)

## 这是什么

Get笔记里的内容适合在应用内阅读，但当你想做个人备份、内容检索、写作素材整理或迁移到 Obsidian / Notion / 本地知识库时，逐篇复制会很耗时。

批量提取文案md提供一个清晰的本地工作流：

1. 打开你指定的 Get笔记知识库链接。
2. 在独立 Chrome 窗口中完成一次登录。
3. 读取左侧文章列表，逐篇打开原文页面。
4. 提取标题、原文地址和正文段落。
5. 按序号和标题保存为 Markdown，并输出本次导出目录。

工具只操作当前登录账号能够看到的页面，不需要把账号密码写进配置文件，也不要求把浏览器 Cookie 上传到 GitHub。

## 主要功能

### 1. SPA 页面适配

Get笔记页面是单页应用，点击文章后不一定改变当前 URL。工具同时支持当前页面跳转和新标签页跳转，并在正文页面渲染完成后再提取内容，避免只拿到摘要或空白页面。

### 2. 完整正文 Markdown

每篇文件包含：

- 一级标题
- 来源 URL
- 正文段落
- 安全文件名（自动替换系统不允许的字符）

输出示例：

```text
exports/
└── Ai江湖策/
    ├── 001_文章标题.md
    ├── 002_文章标题.md
    └── 003_文章标题.md
```

### 3. 独立、持久化登录状态

工具使用单独的 Chrome profile，不占用日常 Chrome 的标签页和登录资料。首次登录后 profile 会保留在本机，后续导出通常可以直接复用。

默认 profile 位置：

- macOS：`~/.batch-extract-md/chrome-profile`
- Linux：`~/.batch-extract-md/chrome-profile`
- Windows：`%USERPROFILE%\\.batch-extract-md\\chrome-profile`

### 4. CLI 与桌面 GUI

- **CLI**：适合批处理、脚本、远程终端和 Agent，标准输出只返回 JSON。
- **GUI**：适合不想记命令的用户，可填写 URL、选择输出目录、限制试运行篇数并查看日志。

### 5. Agent Skill

仓库内置 `skills/batch-extract-md/SKILL.md`，可以安装到 Codex 或 Claude Code。安装后，直接告诉 Agent “导出这个 Get笔记知识库：URL” 即可触发本地命令。

### 6. 试运行与可控范围

支持 `--max 3` 先验证少量文章，再去掉限制做全量导出。输出目录和登录 profile 都可以通过参数指定，方便不同知识库分开保存。

## 可以在哪里使用

### macOS

适合 Apple Silicon 和 Intel Mac。安装 Python 3.11+、Google Chrome 和 Playwright 浏览器内核后，可使用 CLI 或 GUI。

### Windows

适合 Windows 10/11。安装 Python 3.11+、Google Chrome 和 Playwright 浏览器内核后，在 PowerShell 使用同样的命令。

### Linux

适合有桌面环境的 Linux。需要可用的 Chrome/Chromium；无桌面环境时可以使用项目的 CLI 结构，但首次登录仍需要可交互浏览器窗口。

### 本地 Agent 工作流

安装 Skill 后，可在 Codex、Claude Code 等能够运行本地命令的 Agent 中使用。Agent 负责组织参数和返回结果，浏览器登录仍在本机独立窗口中完成。

## 安装

### 方式一：从源码安装

```bash
git clone https://github.com/tieka055-debug/batch-extract-md.git
cd batch-extract-md
python -m venv .venv

# macOS / Linux
. .venv/bin/activate

# Windows PowerShell
# .venv\\Scripts\\Activate.ps1

python -m pip install --upgrade pip
pip install -e .
playwright install chromium
```

检查命令是否可用：

```bash
batch-extract-md --help
```

### 方式二：安装 Agent Skill

在项目目录执行：

```bash
batch-extract-md install-skill --agent codex
# 或安装到 Claude Code
batch-extract-md install-skill --agent claude
```

Skill 只是 Agent 的调用说明；电脑上仍需保留本项目环境，或让 `batch-extract-md` 命令在 PATH 中可用。

## 使用方法

### CLI：从完整 URL 导出

```bash
batch-extract-md export-url \
  "https://www.biji.com/subject/TARGET/DEFAULT?followId=TARGET&followName=NAME"
```

首次执行时：

1. 工具启动独立 Chrome。
2. 在 Chrome 中扫码或登录 biji.com。
3. 登录成功并显示文章列表后，不要关闭窗口。
4. 工具自动逐篇导出。
5. 终端最后输出 JSON，例如：

```json
{"ok": true, "output_dir": "exports/NAME", "exported": 18}
```

### 先导出少量文章验证

```bash
batch-extract-md export-url "URL" --max 3
```

确认标题和正文都正确后，再执行完整导出：

```bash
batch-extract-md export-url "URL"
```

### 指定输出位置

```bash
batch-extract-md export-url "URL" \
  --output "/Users/你的用户名/Documents/Get笔记备份"
```

工具会在输出目录下按 URL 的 `followName` 创建知识库子目录；没有 `followName` 时回退到知识库 ID。

### 指定登录 profile

```bash
batch-extract-md export-url "URL" \
  --profile "/Users/你的用户名/.batch-extract-md/chrome-profile"
```

一般不需要修改该参数。多人共用电脑或需要隔离不同账号时，可以为不同账号指定不同 profile。

### GUI：桌面窗口使用

```bash
python -m biji_archive.gui
```

窗口中填写：

- 知识库 URL
- 输出目录
- 登录 profile 目录
- 最多导出篇数（填 `0` 表示全部）

点击“开始导出”后，在弹出的独立 Chrome 中完成登录即可。

## 使用场景

- 个人备份订阅的知识库文案
- 将短视频文案整理到 Obsidian、Notion 或本地 Markdown 库
- 为写作、选题、课程整理建立本地素材库
- 对 Markdown 文件做全文搜索、标签整理和二次编辑
- 先用少量文章测试，再进行完整归档
- 通过 Agent Skill 让 Codex / Claude Code 自动执行导出命令

## 文件与隐私说明

- 登录 profile 只保存在本机，不应提交到 Git。
- 导出的 Markdown 可能包含原作者内容，请按你的订阅权限和平台规则使用。
- 仓库不内置账号、密码、Cookie、Token 或任何个人数据。
- 建议把导出目录放在自己的备份盘，并加入个人项目的 `.gitignore`。

## 常见问题

### 登录后仍然停在等待状态怎么办？

确认登录是在工具新开的独立 Chrome 窗口中完成，而不是日常浏览器窗口。登录后进入知识库 URL，等待左侧文章列表出现，再让脚本继续。

### 为什么第一次导出建议使用 `--max 3`？

它可以快速验证登录状态、页面选择器和正文结构。验证通过后再做全量导出，避免等待很久才发现页面发生变化。

### 输出目录在哪里？

终端 JSON 的 `output_dir` 就是完整结果目录。默认是当前项目下的 `exports/知识库名称/`。

### 文件名重复怎么办？

工具会自动替换非法字符，并为重名文件追加序号，不会覆盖已有 Markdown。

### 运行中断怎么办？

当前版本会保留已经写入的文件；重新运行时建议使用新的输出目录，或根据已存在文件手动核对。断点续传和更细的失败重试列入后续版本。

### 页面改版后抓不到怎么办？

先保留失败页面和终端错误信息，再用 `--max 3` 复现。页面选择器集中在 `src/biji_archive/exporter.py`，方便后续适配。

## 开发与验证

```bash
python -m pip install -e .
python -m pip install pytest
pytest -q
python -m py_compile src/biji_archive/*.py
```

GitHub Actions 会在 push 和 pull request 时执行测试与 Python 语法检查。

## 路线图

- [ ] 更可靠的断点续传和失败重试
- [ ] 知识库列表、博主列表和选择式导出
- [ ] 可选的接口适配器与 Token 自动刷新
- [ ] 导出 JSON、CSV 和按日期分组的 Markdown
- [ ] macOS / Windows 可下载安装包
- [ ] 发布版本和变更日志

## 许可证

MIT License。
