[project]
name = "biji-archive"
version = "0.1.0"
description = "Personal Get笔记 knowledge-base exporter with browser-rendered fallback"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["playwright>=1.40"]

[project.scripts]
biji-archive = "biji_archive.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
